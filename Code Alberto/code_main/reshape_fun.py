import numpy as np
import pandas as pd

def all_to_one(data_set, strategy, carateristics, vitals, tests, h_obs):
	col_sorted = caracteristics + vitals + tests
	col_to_mod = vitals + test
	#n_patients = (data_set.shape[0]/12).astype(int)
        if strategy :
            a = 1

	
	return data_set_mod
